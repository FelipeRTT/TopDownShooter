room_width = irandom_range(1280,4480); //alterando tamanho da room 
room_height = irandom_range(960,3200);

//alterando background do level
/*
var fundo = choose(fundo1, fundo2);
var layer_id = layer_background_get_id("background");

layer_background_change(layer_id, fundo)